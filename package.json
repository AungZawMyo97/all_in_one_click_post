{
  "name": "all-in-one-social-posting",
  "version": "1.0.0",
  "description": "A unified platform for posting to multiple social media channels",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "build:all": "cd client && npm ci && CI=false npm run build && cd ../server && npm ci",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "setup": "node setup.js",
    "start": "cd server && node index.js"
  },
  "keywords": ["social-media", "posting", "telegram", "viber"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
